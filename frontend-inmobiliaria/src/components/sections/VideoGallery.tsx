// src/components/sections/VideoGallery.tsx
import { Swiper, SwiperSlide } from 'swiper/react';
import { Autoplay, Navigation } from 'swiper/modules';
import 'swiper/css';
import 'swiper/css/navigation';

export default function VideoGallery() {
  return (
    <section className="py-24 bg-[#f8fafc] font-montserrat w-full relative z-10 border-t border-gray-100">
       {/* Pega aquí el código del Swiper y los 3 videos que tienes en el page original */}
    </section>
  );
}